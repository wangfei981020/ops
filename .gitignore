node_modules/
dist/
.turbo/
*.tsbuildinfo
.DS_Store
.env
.env.local

# ⚠️ 仓库根的 .gitignore 有 `*.sql`，会吞掉迁移文件。
# 本目录下如新增 .sql，必须 `git add -f`，否则镜像能跑但 git 里没有。
# 这里显式反否定，避免再踩一次。
!**/migrations/*.sql

# 绝不进仓库：任何密钥实体
*.pem
*.key
!**/*.public.key

# 🔴 文档与问题记录只留本地，不进公开仓库。
#
#	GitHub 上这个仓库只放**干净的代码**：任何排障过程、生产现象、
#	内部编号、集群/服务的真实名字，都可能带出不该公开的信息，
#	而脱敏是逐行人工确认的活 —— 一次疏漏就永久留在提交历史里。
#	（历史上 monorepo 就因此用 git filter-repo 重写过一次远端历史。）
docs/
